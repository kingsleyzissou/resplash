export default ({ setUser, apiKey ***REMOVED***) => {
  const user = window.sessionStorage.getItem(
    `firebase:authUser:${apiKey***REMOVED***:[DEFAULT]`
  );
  if (user) setUser({ authenticated: true, user ***REMOVED***)
***REMOVED***